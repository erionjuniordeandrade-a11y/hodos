# Synthetic case illustrations and release

Authorized outcome: add case-matched synthetic MRI-style illustrations, commit, push and deploy Hodos. Root `/Users/eriondeandrade/hodos`; branch `codex/case-conference-pilot`. Preserve all atlas features, case text/progress and existing schematics. No real patient imaging, tract overlay, new backend or diagnostic claim.

Scope: three original PNGs, case-image metadata/rendering, explicit build allowlist, image-loading tests. Accept when each case loads its matching labelled image on desktop/mobile, existing case flows and unit tests pass, public release hashes match the reviewed build. Broad visual location checks are not independent anatomical approval. Generated anatomy remains illustrative; vignette is authoritative. Stop on claim conflict, failed tests or deployment/auth rejection. Rollback uses prior immutable Pages deployment. User explicitly authorized publication in this turn.
