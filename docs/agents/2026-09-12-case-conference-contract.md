# Additive Case Conference design task

Owner: Dr. Erion de Andrade. Coordinator: Codex. Root: `/Users/eriondeandrade/hodos`; baseline HEAD `482c555`, clean tracked tree. Work is design-only. Claim acquired before documentation edits.

## Goal and source of truth

Consolidate the owner's approved brainstorming decisions into one reviewable design: an additive Case Conference experience for senior neurosurgical residents, initially glioma/intra-axial fictional teaching cases. Existing Hodos structure and functionality must remain. Primary use is case-conference preparation; independent study and attending-led rounds follow later.

## Scope and agents

- Luna, maximum reasoning: read-only inspection of actual integration points and regression boundaries.
- Grok 4.5: one bounded, tool-disabled product/learning review from a sanitized project brief.
- Codex: evidence check, adjudication, design document and closeout. No application implementation, deployment, model-generated medical content release, or public claims.

The owner explicitly requested both agents. Neither may edit the shared checkout. No additional agents or repeated paid/model jobs are authorized by this contract. The Grok CLI model name was verified from its current model inventory; relative pricing was not verified.

## Acceptance and limits

Design records preserve-existing behavior, all three pilot cases, staged reasoning, schematic fiction, spoken/typed parity, expert self-assessment, state/storage boundaries, integration alternatives, a small formative evaluation, and evidence limitations. Agent suggestions are reviewed rather than accepted automatically. Case scripts, anatomical schematics and clinical citations require a separate content-authoring/review step and are not represented as finished.

## Blast radius, rollback, stop conditions

Documentation only under `docs/` and `handoff/`. Do not change `viewer/`, tests, dependencies, existing content or build scripts. Rollback consists of ignoring the new draft documents. Stop if another agent owns the checkout, a design requires patient uploads/registration, or implementation is needed before design approval. No code tests are required for prose-only changes; verify document consistency and Git diff/status. No commit/push without an explicit instruction. No durable personal-memory updates.
